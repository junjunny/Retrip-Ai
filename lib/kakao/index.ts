/**
 * lib/kakao — external-service adapter (PHASE 0 placeholder).
 *
 * All kakao network calls will live here so that pages / features never talk to
 * the external API directly. Nothing is implemented yet.
 */
export {};
