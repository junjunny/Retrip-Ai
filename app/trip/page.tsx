/**
 * /trip — placeholder route (PHASE 0).
 *
 * Trip creation / viewing arrives in Phase 1.
 */
export default function TripPage() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-2xl flex-col justify-center gap-2 p-8">
      <h1 className="text-xl font-semibold">Trip</h1>
      <p className="text-sm text-zinc-500">Phase 1 — not implemented yet.</p>
    </main>
  );
}
