/**
 * Static application configuration (non-secret).
 */
export const appConfig = {
  name: "Re:Trip AI",
  slogan: "Your Plan Can Change. Your Trip Doesn't Have To.",
  /** Current development phase — see README "Development Phases". */
  phase: 1,
} as const;
